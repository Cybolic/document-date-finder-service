services:
  # A Docker web service
  - type: web
    name: superdoc-fastapi-example
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: uv sync --frozen --no-cache
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
